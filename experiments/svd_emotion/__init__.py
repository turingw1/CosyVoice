"""SVD-based emotion / semantic decomposition experiment for CosyVoice3 flow.

See /Users/.../Voice/plan.md (v2) for the full plan.

Convention (do not flip):
- B (top-k SVD reconstruction) = semantic baseline (high energy)
- A (residual)                  = emotion component (low energy)
"""
